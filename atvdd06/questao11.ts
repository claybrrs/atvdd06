// 11. Crie um programa que leia uma sequência de números inteiros do usuário e exiba o maior e o
// menor valor digitado. O programa deve parar de ler quando o usuário digitar o número 0
// utilizando o while.

// let maior = null;
// let menor = null;
// let numero = null;

// while (numero !== 0) {
//   numero = parseInt(prompt('Digite um número inteiro (0 para parar): '), 10);

//   if (numero === 0) {
//     break;
//   }

//   if (maior === null || numero > maior) {
//     maior = numero;
//   }

//   if (menor === null || numero < menor) {
//     menor = numero;
//   }
// }

// if (maior !== null && menor !== null) {
//   alert(O maior valor digitado foi: ${maior});
//   alert(O menor valor digitado foi: ${menor});
// } else {
//   alert('Nenhum valor foi digitado.');
// }